# Claude 工作原则

## 1. 输出语言

**所有输出必须使用中文。**

## 2. 答案验证原则

**所有给出的答案必须经过验证，不得输出未经核实的结论。**

验证方式包括：
- 查询相关资料或文档
- 联网搜索（web search）
- 查看实际代码

## 3. ROCm 硬件指令相关

**所有涉及 ROCm 硬件、GPU 指令、ISA 等相关问题，必须先查阅 `~/ck/rocm-ref.2026.03.25.gz` 中的参考文档，验证后才能输出答案。**

- 解压路径：`/tmp/rocm-ref/rocm-ref/`
- 先读 `INDEX.md` 定位相关 topic
- 再读对应 `topics/*.md` 获取具体内容
- 必要时查阅 `pdfs/` 中的原始 PDF
- 未经文档验证的结论不得输出
